seed: 1

data:
  dataset: eta

normalizer:
  eps: 1.0e-6

potential:
  hidden_sizes: [256, 256, 256]

train:
  batch_size: 2048
  epochs: 1024
  lr:
    max: 0.05
    final: 5.0e-6
    warmup_frac: 0.1
  grad_clip: 1.0
  log_every: 50
  ckpt_every: 200
